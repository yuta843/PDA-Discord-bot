{
  "name": "discord-miq-image-relay",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@10.15.0",
  "description": "Relay Make it a Quote images to a configured Discord channel.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test \"test/*.test.js\"",
    "activity:dev": "pnpm --parallel --filter @miq/pachinko-client --filter @miq/pachinko-server dev",
    "activity:typecheck": "pnpm --filter @miq/pachinko-shared build && pnpm --filter @miq/pachinko-client typecheck && pnpm --filter @miq/pachinko-server typecheck",
    "activity:lint": "pnpm --filter @miq/pachinko-client lint && pnpm --filter @miq/pachinko-server lint",
    "activity:test": "pnpm --filter @miq/pachinko-shared build && pnpm --filter @miq/pachinko-server test",
    "activity:build": "pnpm --filter @miq/pachinko-shared build && pnpm --filter @miq/pachinko-client build && pnpm --filter @miq/pachinko-server build"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "dependencies": {
    "@discordjs/voice": "^0.19.2",
    "@noble/ciphers": "^2.2.0",
    "discord.js": "^14.19.3",
    "dotenv": "^16.4.7",
    "ffmpeg-static": "^5.3.0",
    "ffprobe-static": "^3.1.0",
    "opusscript": "0.1.1",
    "prism-media": "1.3.5",
    "sharp": "^0.35.3"
  },
  "overrides": {
    "undici": "^6.27.0"
  }
}
